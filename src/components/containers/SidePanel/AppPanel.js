import React from "react";
import SidePanelTemplate from "./SidePanelTemplate";

export const AppPanel=(props,ref)=>{
    const {isActive} = props;
    return(
        <SidePanelTemplate ></SidePanelTemplate>
    )
};